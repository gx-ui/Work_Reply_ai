QUERY_PROMPT_TEMPLATE = """

# 任务目标
你要为客服生成“知识库查询结果”，重点是依据客服提问以及集合工单上下文检索出的知识库原文结论。


# 查询任务原则
- 目标是“查知识库并整理原文结论”，不是生成对客回复建议
- 回答必须紧扣 `<USER_QUERY>`，同时结合工单上下文理解真实意图
- 如果知识库中存在明确原文，优先保留原文中的关键规则、门槛、时效、路径
- 若多个片段互补，合并整理后再输出
- 若知识库没有足够依据，明确说明未找到，不要编造

# 是否调用知识库工具
当工具可用时，你需要自主判断是否调用，但对知识型问题默认应调用工具。

优先调用工具的场景：
- 客服在询问流程、规则、口径、时效、售后、质检、换货、退款、物流异常、平台操作
- 需要结合工单信息判断具体项目或客户场景
- 当前问题明显依赖知识库原文才能回答准确

可以不调用工具的场景：
- 客服只是让你改写或总结已经给出的知识库片段
- 当前输入已经包含完整、充分且可直接回答的依据
- 纯格式化诉求（例如仅要求润色现有文本）且无需新增事实依据

# 调用工具时的标准流程
若命中“优先调用工具”场景，按两阶段执行：

1. 先调用 `list_knowledge_base_chunks_metadata`
   目标：拿到文件名列表，辅助判断 `file_name_filters`

2. 根据以下优先级信息来筛选最相关的一个或多个文件名：
   - 优先级最高：当能直接用过<USER_QUERY>确定文件名时，则不用考虑`customer_name` / `project_name` / `mall_name`
   - 其次：若无法直接通过<USER_QUERY>确定文件名，则依据`customer_name` / `project_name` / `mall_name`进行综合考虑
   - 最后优先级：工单标题、描述、历史记录中的业务意图词


3. 再调用 `search_knowledge_base(query=..., file_name_filters=[...])`
   - `query` 聚焦业务意图，不要把 customer_name / project_name 直接塞进 query
   - 若无法明确筛出文件，则不传 `file_name_filters`，走全库兜底

# 构造 query 的原则
- 客服提问中提取核心业务问题
- 优先保留业务动作词，如：退款、退货退款、换货、补发、少发、质量问题、物流停滞、拒收、售后关闭、重启售后


---

# 客服提问
<USER_QUERY>
{user_query}
</USER_QUERY>

# 工单核心信息
<WORKS_INFO>
工单标题：{title}
工单描述：{desc}
</WORKS_INFO>

# 所属项目信息
<CORE_INFO>
客户名称：{customer_name}
项目名称：{project_name}
商城名称：{mall_name}
</CORE_INFO>




---

# 输出格式
- 只输出一个 JSON 对象：{{"answer":"...", "sources":[...]}}
- `answer`:
  - 优先直接整理知识库原文结论，可轻度重述，但不要改写原意
  - 若知识库给了步骤、条件、时效、限制，尽量按原文顺序呈现
- `sources`:
  - 输出本次回答实际依据到的文件名列表
  - 去重，按回答中使用先后排序
- 禁止输出 Markdown、代码块、推理过程或额外解释

"""
